<table>
  <tr>
    <th>Item</th>
    <th>Quantity</th>
    <th>Description</th>
  </tr>
<% @items.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.quantity %></td>
    <td><%= item.description %></td>
  </tr>
<% end %>
</table>
