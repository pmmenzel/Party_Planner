<h1>Items#new</h1>

<%= form_for @item, url: {action: "create"} do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_field :description %>

<%= f.label :quantity %>
<%= f.number_field :quantity %>

<%= f.submit "Create" %>
<% end %>

