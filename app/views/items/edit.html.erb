<h1>Items#edit</h1>

<%= form_for @item, url: {action: "update"} do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_field :description %>

<%= f.label :quantity %>
<%= f.number_field :quantity %>

<%= f.submit "Update" %>
<% end %>

